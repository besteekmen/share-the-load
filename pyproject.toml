[project]
name = "share-the-load"
version = "0.1.0"
description = "A tool for managing shared household chores"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "Django==6.1.1",
]
